# baseball_seq

研究問題：打者可以依「對球的反應」分群；對每一群，找出最有效的配球「序列」
（不只是單一球種或位置，而是前幾球怎麼鋪陳）。

資料來源是 2015-2026 年 MLB Statcast 逐球資料。


## 文件結構

```
baseball_seq/
  data_preparation/
    data_spec.py         ✅ 規格：要抓什麼、特徵怎麼算、目標怎麼定義（不做任何 I/O）
    fetch_data.py        ✅ 下載 Statcast 與球員身高體重          → dataset/raw/
    preprocess.py        ✅ 標籤、正規化、情境、序列特徵            → dataset/interim/pitches_{年}.parquet
    batter_profile.py    ✅ 打者輪廓：上一季 + 近 30 天             → dataset/interim/batter_*_{年}.parquet
    splits.py            ✅ 依球季切 train/val/test、定義預測目標   → dataset/splits/manifest.json
    dataset/             資料（不進 git）
  modeling/
    features.py          ✅ 把逐球資料轉成模型輸入張量              → dataset/splits/arrays/{年}/
    outcomes.py          ✅ 三段式預測 -> 場上事件、球數轉移、得分價值 → dataset/splits/run_values.json
    model.py             ✅ Transformer 序列模型，輸出 7 類事件機率
    train.py             ✅ 依 config 訓練，輸出到 runs/<名稱>_<時間>/
    evaluate.py          ✅ 整體、三段式、分層、校準、歷史消融
    curves.py            ✅ 各情況的 ROC 曲線與指標表格 (Precision、Recall、F1 ...)
  configs/
    event7_base.yaml     ✅ 基準實驗設定
  analysis/
    counterfactual.py    ⬜ 尚未建立
  tests/                 ✅ 資料洩漏與特徵一致性的測試
  runs/                  每次訓練的輸出（不進 git）
```


## 執行順序

每一步都可以重跑；已完成的部分會跳過或覆寫，不會重複下載。

```bash
source .venv/bin/activate

python data_preparation/fetch_data.py --season all       # 1. 下載 (約 1.9 GB，已下載的月份會跳過)
python data_preparation/preprocess.py --season all       # 2. 逐季前處理 (每季約 8 秒)
python data_preparation/batter_profile.py --season all   # 3. 打者輪廓 (全部約 20 秒)
python data_preparation/splits.py                        # 4. 檢查切分、輸出分布比較
python -m modeling.features --fit --build                # 5. 標準化參數 + 張量快取 (約 1.5 分鐘，11 GB)
python -m modeling.outcomes --fit                        # 6. 各球數 x 事件的得分價值表 (只用 train)
python -m modeling.train --config configs/event7_base.yaml         # 7. 訓練 (結束後自動在 val 上評估)
python -m modeling.curves --run runs/<run> --split val              # 8. 各情況 ROC 曲線與指標表格
python -m modeling.evaluate --run runs/<run> --split test          # 9. 模型定案後才在 test 上評估
python -m modeling.curves --run runs/<run> --split test

python -m pytest tests/                                  # 測試
```


## 配球策略 UI

本機網頁介面可搜尋 2026 年資料中的投手與打者，以右／左打站位篩選打者，執行
`k_sequence`，並比較最佳應變、最佳固定、貪婪、投手實際傾向、隨機配球與真實打席。

```bash
.venv/bin/python -m ui.server
# 瀏覽器開啟 http://127.0.0.1:8765
```

直接使用虛擬環境裡的 Python，可以避免系統的 `python3`（例如 Ubuntu 20.04 的
Python 3.8）誤跑 UI。若要先啟用環境，也可以執行
`source .venv/bin/activate` 後再用 `python -m ui.server`。

分析在背景程序執行，瀏覽器會持續顯示搜尋進度。UI 固定使用時間戳最新、且
`model.pt`／`config.yaml`／`normalizer.json` 齊全的 run，避免不同模型的輸入規則與快取混用；
相同投手與打者的新版結果會讀取 `runs/<最新 run>/k_sequence_ui/` 中的快取。結果頁包含：

- 分析完成即寫入磁碟並刷新「已完成的分析」；server 重啟後會自動掃描恢復，舊版結果保留為唯讀歷史
- 1–5 球內累積三振率與所有策略的結果比較
- 好球帶九宮格與四個帶外區域的正式位置名稱
- 每種推薦策略可從比較表直接展開，以球種顏色、投球順序與左右打者投影呈現實際代表球落點
- 最佳固定五球序列與前幾名候選
- 最佳應變策略的可收合決策樹（球數、建議球路、打者反應與條件機率）
- 目前顯示深度的策略樹 SVG 下載
- 分析完成後自動下載完整結果 ZIP（JSON、Markdown、CSV 與機率曲線圖），也可手動重載
- 原始完整 Markdown 報告

也可不經 UI，直接指定任意球員執行：

```bash
python -m analysis.k_sequence \
  --run runs/<run> \
  --pitcher-id 808967 --pitcher-name 山本由伸 \
  --batter "592450:Aaron Judge" \
  --out runs/<run>/k_sequence_ui/808967/592450
```


## 各檔案做了什麼

### data_preparation/data_spec.py
整個專案的「單一事實來源」。只描述要什麼，不讀寫任何檔案。

- 路徑、賽季範圍 (2015-2026)、各欄位從哪一年開始有資料 (`COLUMN_AVAILABILITY`)
- 要保留的 Statcast 欄位與用途
- 球種分族 (`PITCH_FAMILY`：16 種球種收斂成 6 族) 與要剔除的非競技球種
- 三段式預測目標的精確定義：
  - Stage 1 `P(揮棒)`
  - Stage 2 `P(觸球 | 揮棒)`，界外球算觸球
  - Stage 3 `擊球初速 | 觸球`
- 衍生特徵的定義與防洩漏規則 (`DERIVED_FEATURES`)

其他檔案的常數與規則都應該從這裡讀，不要各自寫一份。

### data_preparation/fetch_data.py
下載原始資料到 `dataset/raw/`。

- **Statcast 逐球資料**：逐月下載、每月一個 parquet，再合併成年檔 `statcast_{年}.parquet`
  - 已經完整的月份會跳過；沒有比賽的月份寫下 `.empty` 標記，之後不再查詢
  - 整月查詢失敗時自動改成逐日下載
- **球員資料** (`player_bio.parquet`)：身高、體重、慣用手、守備位置，來自 MLB Stats API
- 只做最低限度的過濾：只留例行賽、剔除牽制等非競技投球

### data_preparation/preprocess.py
原始逐球資料 → `dataset/interim/pitches_{年}.parquet`（每季約 70 萬球、167 欄）。

**不刪列、只標記**：直接刪掉觸擊或缺值的球，會讓後面的球找錯「前一球」，所以一律保留並加旗標。

| 類別 | 內容 |
|---|---|
| 標籤 | 三段式 (`is_swing`、`is_contact`、`ev_measured`)；三類擊球 `contact3` (0 未打進場 / 1 弱擊 / 2 強擊 >95 mph / -1 打進場缺初速)；每球反應 `pitch_outcome` |
| 正規化 | `plate_z_norm` (0 = 好球帶下緣、1 = 上緣)、`plate_x_norm`、**打者視角** `plate_x_bv` / `pfx_x_bv` (左打翻正，正 = 外角)、`in_zone` (Savant 官方 zone) |
| 投手 | `speed_vs_own_fastball`：本球球速減投手速球均速，均速只用本場之前的比賽 |
| 情境 | 12 類球數、壘包狀態、base-out 狀態、棒次、本場第幾次對這位投手、投手用球數 |
| 打線脈絡 | 前 1-3 個打席的結果、是否上壘、本半局已面對幾位打者（只用嚴格更早的打席） |
| 序列 | 前 1-3 球的特徵、速差、進壘點差、位移差、球種配對、出手點距離、tunnel ratio |
| 品質旗標 | `pa_valid` (無缺值、無斷號、無觸擊)、`is_model_target` (可當預測目標) |

棒次以「打者身分」推導，不是把打席編號 mod 9：只要有一個打席在資料裡沒有任何一球（例如不投球的故意四壞），mod 9 的作法會讓後面整場的棒次錯一格。

### data_preparation/batter_profile.py
打者輪廓，兩種時間基準、兩份輸出：

**1. 上一季輪廓** `batter_prev_season_{年}.parquet`（一位打者一列，只讀前一年的資料）
- 整體：揮棒率、揮空率、追打率、打進場率、強擊率、平均初速、拉打率
- 揮棒機制：揮棒速度、快速揮棒比例 (≥75 mph)、揮棒長度、攻擊角。沒有 bat tracking 的年份是 NaN，不是 0
- 6 個球種族各自的追打率、揮空率、強擊率、平均初速
- 球種族 × 4 個區域 (好球帶上 / 中 / 下 / 追打區) 的揮棒率、揮空率
- 一列 `batter = -1` 的聯盟平均，給沒有上一季資料的打者用

**2. 近期輪廓** `batter_recent_{年}.parquet`（打者 × 比賽日）
- 比賽日**之前** 30 天的拉打率，整體與各球種族
- 窗口內揮棒不到 50 次時退回上一季值

所有比率都向聯盟平均收縮，樣本越少越接近聯盟。
`attach_batter_profile()` 負責合併到逐球資料：新人填聯盟平均並標記 `is_rookie`。

### data_preparation/splits.py
依**球季**切分，不隨機切（隨機切會讓同一季的球同時出現在 train 和 test）。

| 切分 | 球季 | 用途 |
|---|---|---|
| train | 2016-2023 | 訓練（2015 沒有上一季輪廓，不納入） |
| val | 2024 | 選超參數、early stopping |
| test | 2025-2026 | 最終評估，定案後只看一次 |

- 不複製資料，只定義規則。`load_season()` 讀一季並合併打者輪廓；`iter_split()` 逐季讀取
- `use_as_target`：`is_model_target` 且有上一季輪廓，並排除投手打擊（投手打擊的球仍保留在序列中當脈絡）
- 執行時會檢查：季別不重疊、依時間先後、每季的打者輪廓只來自前一季；並輸出各切分的標籤分布比較

### modeling/features.py
把逐球資料轉成模型輸入張量。**訓練、評估、反事實分析都必須呼叫這一份**，不在別處重寫。

每個樣本是一顆可當目標的球：

| 張量 | 形狀 | 內容 |
|---|---|---|
| `token_num` | [N, 21, 22] | **本打席**第 1 球到本球（本球在最後一格，不足 21 球的在前面補位）：物理量、與前一球的差異、歷史球初速、旗標 |
| `token_cat` | [N, 21, 2] | 球種族、打者反應（本球的反應被遮蔽成 `<mask>`） |
| `pad` | [N, 21] | 補位遮罩：第 N 球的樣本只有最後 N 格是真的球 |
| `ctx_num` | [N, 104] | 情境、上一季輪廓、近期拉打率、新人旗標 |
| `ctx_cat` | [N, 4] | 球數、base-out 狀態、投打慣用手組合 (RHP_RHB / RHP_LHB / LHP_RHB / LHP_LHB)、前一打席結果 |
| 標籤 | [N] | `y_swing`、`y_contact` + `m_contact`、`y_ev` + `m_ev`、`y_contact3` + `m_contact3`（`m_*` 是各段的母體遮罩） |

- **差異特徵在窗口內即時計算**，不讀 preprocess 的 `prev1_*` 欄位。反事實替換某一顆球後，下一顆球的速差、tunnel ratio 會自動更新
- **標準化參數只從 train 估計**，存在 `dataset/splits/normalizer.json`，val / test / 反事實一律載入同一份；缺值標準化後填 0
- **類別詞彙寫死在程式裡**，不從資料學，確保各切分編碼一致
- **揮棒機制特徵預設不使用**：train 年份的輪廓來源都沒有 bat tracking，放進去會造成 val / test 才突然有值的分布偏移。需要時加 `--use-bat-tracking`
- **序列只含同一打席**：第 N 球的序列就是本打席第 1 到第 N 球，不會混入前一個打席。2015-2026 最長的打席是 21 球，所以窗口開到 21，沒有任何打席被截斷
- **投打慣用手明確給出**：位置與位移特徵已翻成打者視角，另外用 4 類 `matchup` 告訴模型實際的投手與打者慣用手
- 快取：每季一個資料夾 `dataset/splits/arrays/{年}/`，每個張量一個 `.npy` (`token_num` 存 float16)，用 `load_cached()` 以 mmap 讀取，全部約 11 GB

### modeling/outcomes.py
把三段式預測對應到場上實際發生的事件，是模型輸出和「配球好不好」之間的橋樑。

三段式只給 `P(揮棒)`、`P(觸球 | 揮棒)`、`初速 | 觸球`，要決定球數怎麼走、打席有沒有結束，還需要另外四個條件機率：

| 條件機率 | 為什麼需要 | 2023-2025 實際值 |
|---|---|---|
| `P(判好球 \| 沒揮棒)` | 沒揮棒時是壞球 (b+1) 還是好球 (s+1) | 好球帶內 88.8%、帶外 5.4% |
| `P(打進場 \| 觸球)` | 界外 (打席繼續) 還是打進場 (打席結束) | 約 48%，幾乎不隨好球數變 |
| `P(擦棒被捕 \| 界外類觸球)` | 兩好球時擦棒被接住就是三振 | 約 5%；佔三振的 6.9% |
| `P(初速 > 95 \| 打進場)` | 強擊只對打進場的球有意義 | 39.5%（界外球只有 6.6%） |

事件樹 (7 種事件)：

```
投球
├─ 沒揮棒 ─┬─ 壞球            b+1；三壞球 -> 保送
│          └─ 好球            s+1；兩好球 -> 三振
└─ 揮棒 ───┬─ 揮空            s+1；兩好球 -> 三振
           └─ 觸球 ─┬─ 擦棒被捕  s+1；兩好球 -> 三振
                    ├─ 界外      兩好球以下 s+1；兩好球時不變
                    └─ 打進場 ─┬─ 強擊 (>95 mph)   打席結束
                               └─ 弱擊            打席結束
```

- `event_probs()`：條件機率 -> 7 種事件的機率
- `transition()` / `step()`：依棒球規則推進球數分布，算出打席在每一球結束 (三振 / 保送 / 強擊 / 弱擊) 的機率
- `event_label()`：Statcast description -> 事件名稱
- `fit_run_values()`：從 train 估計每個 (球數, 事件) 的平均 `delta_run_exp`，讓不同結果能放在同一把尺上比較，例如 0-0 強擊 +0.292、3-2 看好球 -0.331

### 預測目標：7 類事件
模型不分段訓練，而是直接輸出每一球 7 類事件的機率 (softmax)：
`壞球、好球、揮空、擦棒被捕、界外、弱擊 (打進場 ≤95 mph)、強擊 (打進場 >95 mph)`

- 三段式的條件機率都可以從這 7 個機率換算，例如 `P(揮棒) = 揮空 + 擦棒 + 界外 + 弱擊 + 強擊`、`P(強擊 | 打進場) = 強擊 / (弱擊 + 強擊)`
- 強擊只看**打進場**的球，不用「觸球上的初速迴歸」：界外球初速在早期年份覆蓋率很低，而且配球決策在乎的是打進場的強擊
- 觸擊、觸身球、打進場但缺初速的球不當標籤 (`m_event = False`)

### 模型輸入完整清單
每個樣本是一顆要預測的球。模型看到的全部資訊如下，定義在 `modeling/features.py`。

#### 1. 序列：本打席第 1 球到本球（每顆球一格，最長 21 格）

**每顆球的數值特徵（22 個，`token_num`）**

| 分類 | 特徵 | 說明 |
|---|---|---|
| 物理量 (11) | `plate_x_bv` | 進壘水平位置，打者視角（正 = 外角） |
| | `plate_z_norm` | 進壘高度，以打者好球帶正規化（0 = 下緣、1 = 上緣） |
| | `release_speed` | 出手球速 |
| | `effective_speed` | 考慮延伸後的等效球速 |
| | `speed_vs_own_fastball` | 本球球速 − 投手本季至前一場的速球均速 |
| | `pfx_x_bv` | 水平位移，打者視角 |
| | `pfx_z` | 垂直位移 |
| | `release_spin_rate` | 轉速 |
| | `release_extension` | 延伸距離 |
| | `release_pos_x_bv` | 出手點水平位置，打者視角 |
| | `release_pos_z` | 出手點高度 |
| 與前一球的差異 (8) | `d_release_speed` | 速差 |
| | `d_plate_x_bv`、`d_plate_z_norm` | 進壘點水平差、垂直差 |
| | `d_pfx_x_bv`、`d_pfx_z` | 水平位移差、垂直位移差 |
| | `plate_dist` | 進壘點距離 |
| | `release_dist` | 出手點距離 |
| | `tunnel_ratio` | 進壘點距離 ÷ 出手點距離（越大越難辨識） |
| 結果 (1) | `ev_measured` | 擊球初速；**本球遮蔽為 0** |
| 旗標 (2) | `has_prev` | 同打席前面是否還有球 |
| | `ev_present` | 是否量得到初速；**本球遮蔽為 0** |

**每顆球的類別特徵（2 個，`token_cat`）**

| 特徵 | 類別 |
|---|---|
| `pitch_family` | 速球、伸卡、卡特、滑球、曲球、變速球、其他 |
| `pitch_outcome` | 壞球、看好球、揮空、界外、打進場、觸身球；**本球遮蔽為 `<mask>`** |

另有 `pad` 遮罩：第 N 球的樣本只有最後 N 格是真的球，前面是補位。

#### 2. 情境：本球當下（`ctx_num` 104 個數值 + `ctx_cat` 4 個類別）

**比賽情境（13 個數值）**

| 特徵 | 說明 |
|---|---|
| `inning` | 局數（10 局以上視為 10） |
| `outs_when_up` | 出局數 |
| `n_runners` | 壘上人數 |
| `lineup_slot` | 棒次 |
| `nth_pa_vs_pitcher` | 本場第幾次面對這位投手 |
| `pitcher_pitch_count` | 投手本場用球數（含本球） |
| `n_thruorder_pitcher` | 投手第幾輪面對打線 |
| `bat_score_diff` | 進攻方分差（限制在 ±10） |
| `pitcher_days_since_prev_game` | 投手距上次出賽天數（上限 30） |
| `inning_batters_faced` | 本半局已面對幾位打者 |
| `prev_pa_reached_count` | 前 3 個打席有幾人上壘 |
| `age_bat`、`age_pit` | 打者年齡、投手年齡 |

**打者上一季輪廓（82 個數值，只用前一季資料，向聯盟平均收縮）**

| 分類 | 數量 | 特徵 |
|---|---|---|
| 整體 | 7 | 揮棒率、揮空率、追打率（好球帶外揮棒）、打進場率、強擊率（打進場 >95 mph）、拉打率、平均初速 |
| 分高度 | 3 | 好球帶上、中、下的強擊率 |
| 分球種族 | 24 | 6 個球種族（速球、伸卡、卡特、滑球、曲球、變速球）× 追打率、揮空率、強擊率、平均初速 |
| 球種族 × 區域 | 48 | 6 個球種族 × 4 個區域（好球帶上、中、下、追打區）× 揮棒率、揮空率 |

**打者近期輪廓（7 個數值）**

| 特徵 | 說明 |
|---|---|
| `recent_pull_rate` | 比賽日前 30 天（不含當天）的拉打率；揮棒不到 50 次時退回上一季值 |
| `recent_pull_rate_{球種族}` × 6 | 各球種族的近期拉打率 |

**旗標（2 個數值）**

| 特徵 | 說明 |
|---|---|
| `is_rookie` | 沒有上一季資料，輪廓填聯盟平均 |
| `recent_window_sufficient` | 近 30 天揮棒至少 50 次 |

**類別（4 個，`ctx_cat`）**

| 特徵 | 類別 |
|---|---|
| `count_state` | 12 種球數（訓練時 30% 機率遮蔽） |
| `base_out_state` | 壘包 × 出局，24 種 |
| `matchup` | 右投右打、右投左打、左投右打、左投左打 |
| `prev1_pa_result_class` | 前一打席結果：無、出局、三振、保送或觸身、一壘安打、長打 |

#### 3. 刻意沒有放進模型的資訊

| 資訊 | 原因 |
|---|---|
| 打者揮棒機制（揮棒速度、快速揮棒比例、揮棒長度、攻擊角） | 2024 年以後才有；train 年份的輪廓全是缺值，放進去會造成分布偏移。可用 `--use-bat-tracking` 開啟 |
| 投手、打者、捕手的身分（ID） | 尚未加入 |
| 轉軸角度 `spin_axis`、內外野佈陣 | preprocess 有保留，尚未放進模型 |
| 身高、體重 | 好球帶正規化後幾乎沒有額外資訊 |
| 本球的結果與初速 | 那是要預測的目標 |

### modeling/model.py
`PitchSequenceModel`：
- **序列**：每顆球 = 數值特徵投影 + 球種族 embedding + 打者反應 embedding + 位置 embedding，經過帶 padding mask 的 Transformer encoder
- **摘要**：本球的表示 + 整段序列的 attention pooling
- **情境**：數值特徵投影 + 球數、壘包出局、投打慣用手組合、前一打席結果的 embedding
- **輸出**：MLP -> 7 個 logits

### modeling/train.py
`python -m modeling.train --config configs/xxx.yaml`
- 讀 features 的快取張量 (train 2016-2023、val 2024)，整份放上 GPU
- AdamW + bfloat16 混合精度；val loss 沒進步時學習率減半，連續 `patience` 次沒進步就停
- `count_dropout`：訓練時隨機把球數換成 `<pad>`，逼模型從序列讀資訊
- 輸出 `runs/<名稱>_<時間>/`：`config.yaml` (含特徵設定與切分)、`normalizer.json`、`model.pt` (val 最佳)、`history.jsonl`、`train.log`
- 訓練結束自動在 val 上跑 `evaluate.py`

### modeling/evaluate.py
`python -m modeling.evaluate --run runs/<run> --split val|test`，輸出 `eval_<split>/metrics.json` 與 `report.md`：

| 項目 | 內容 |
|---|---|
| 整體 | 7 類 log loss，和「只用類別比例猜」的基準比較 |
| 各事件 | one-vs-rest AUC、PR-AUC、平均預測 vs 實際 |
| 三段式 | 揮棒、判好球\|沒揮棒、觸球\|揮棒、打進場\|觸球、強擊\|打進場、強擊 (所有球)，各在自己的母體上評估 |
| 分層 | 強擊與揮棒，依球種族、球數、投打慣用手組合、本打席第幾球分組 |
| 校準 | 強擊與揮棒的十分位校準表 |
| 歷史消融 | 把前面的球換成別的打席的，AUC 掉多少、預測變多少：檢查模型有沒有真的用到序列 |

test 只在模型定案後看，選模型、調參數一律看 val。

### modeling/curves.py
`python -m modeling.curves --run runs/<run> --split val|test`，輸出 `curves_<split>/`。

**17 個問題**（每個都是二元判斷，在自己的母體上評估）：

| 類別 | 問題 |
|---|---|
| A 七種事件 | 壞球、看好球、揮空、擦棒被捕、界外、弱擊、強擊（one-vs-rest，母體為所有球） |
| B 三段式 | 揮棒、判好球\|沒揮棒、觸球\|揮棒、打進場\|觸球、強擊\|打進場 |
| C 組合指標 | CSW（看好球 + 揮空）、好球、打進場、追打（母體：好球帶外）、好球帶內揮棒（母體：好球帶內） |

**8 個分層維度**：全部、球種族、球數（第一球 / 投手領先 / 平手 / 打者領先 / 滿球數）、投打慣用手、本打席第幾球、好球帶內外、球季、打者類型（新人 / 老將）

**門檻**：在 val 上取每個問題 F1 最大的門檻，存成 `runs/<run>/thresholds_val.json`；評估 test 時沿用這份門檻，不在 test 上調整。

| 輸出 | 內容 |
|---|---|
| `metrics.csv` | 每列 = 問題 × 分組：樣本數、正例數、正例比例、平均預測、AUC、PR-AUC、PR-AUC 倍數、Brier、log loss、門檻、Precision、Recall、F1、Specificity、Accuracy、被標記比例、TP/FP/FN/TN |
| `summary.md` | 17 個問題「全部」那一列的摘要表 |
| `roc_overview.png` | 17 個問題的整體 ROC |
| `roc_<問題>.png` | 單一問題，8 個分層維度各一格，同維度的分組疊在一起，並標出門檻的操作點 |

分組樣本少於 200 時不計算 AUC 也不畫曲線。

### tests/
| 檔案 | 檢查內容 |
|---|---|
| `test_preprocess.py` | 標籤定義、左打翻正、速球均速不偷看當天、棒次缺打席時自動校正、打線脈絡不含本打席、序列不跨打席、斷號與觸擊整個打席標為無效 |
| `test_batter_profile.py` | 上一季輪廓只讀前一年、30 天窗口排除當天與更早的比賽、樣本不足時退回、無 bat tracking 為 NaN、新人填聯盟平均、拉打方向 |
| `test_features.py` | 窗口不跨打席、本球被遮蔽、反事實替換後差異特徵跟著更新、缺值填 0、標籤遮罩、切分必須依時間且不重疊 |
| `test_outcomes.py` | 事件機率總和為 1、球數轉移規則 (三壞保送、兩好球界外不變、兩好球擦棒三振)、推進球數時機率守恆 |


## 已知限制

- **2015-2016 年界外球初速覆蓋率很低**（2015 年約 21%，2024 年以後約 88%）。Stage 3 若包含界外球，早期球季的樣本會少很多且不是隨機缺失；三類擊球標籤只看打進場的球，不受影響
- **揮棒機制只有 2024 年以後**，所以只有 2025 年以後的上一季輪廓有值，目前 train 用不到
- **2020 年短球季**只有約 26 萬球
- **2026 年資料到 2026-09-15**，併入 test
- **環境**：`requirements.txt` 的 PyTorch 是 CUDA 12.1 版本，換機器時要依驅動版本調整
- **`tunnel_ratio` 分布右偏**（平均 10.4、標準差 8.4），建模時可考慮先取 log
